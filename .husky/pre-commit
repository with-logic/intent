#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Lint and format only staged files (auto-fixes and re-stages them)
npx lint-staged

# Run full pre-commit checks (typecheck, tests, and build)
npm run pre-commit
